---
all:
  hosts:
    lb-1:
      ansible_host: 192.168.50.91
   
  children:
    loadbalancer:
      hosts:
        lb-1:

  vars:
    ambiente: dev
    virtual_ip: false
    ansible_ssh_private_key_file: ./keys/kubespray
    
